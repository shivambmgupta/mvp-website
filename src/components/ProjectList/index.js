import React, { memo } from 'react';

const ProjectList = () => (
    <div>
        <h1>Projects Here</h1>
        <p>This screen is still under dev</p>
    </div>
);

export default memo(ProjectList);
